EXPORT TAB V2 VIEWPORT FIX

Only src/export.css was changed. The export panel now scopes a bounded app/workspace viewport while mounted, preventing the canvas/workspace from extending underneath the application footer on shorter desktop browser viewports. No editor logic or export engine was changed.
